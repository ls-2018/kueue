---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: multikueueconfigs.kueue.x-k8s.io
spec:
  group: kueue.x-k8s.io
  names:
    kind: MultiKueueConfig
    listKind: MultiKueueConfigList
    plural: multikueueconfigs
    singular: multikueueconfig
  scope: Cluster
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: MultiKueueConfig 是 multikueue API 的 Schema。
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MultiKueueConfigSpec 定义了 MultiKueueConfig 的期望状态。
            properties:
              clusters:
                description: ClusterQueue 中的工作负载应分发到的 MultiKueueCluster 名称列表。
                items:
                  type: string
                maxItems: 10
                minItems: 1
                type: array
                x-kubernetes-list-type: set
            required:
            - clusters
            type: object
        type: object
    served: true
    storage: true
