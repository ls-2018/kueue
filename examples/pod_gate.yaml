apiVersion: v1
kind: Pod
metadata:
  name: title
  labels:
    role: title
spec:
  containers:
    - name: title
      image: nginx
      ports:
        - containerPort: 80
          protocol: TCP
  restartPolicy: Always
  schedulingGates:
    - name: test
